# python-fundamentals
# Notes
# 25-May-2026


# Υπολογιστές και Προγράμματα (Computers and Programs)

---

## 1. Συνοπτική Θεωρία (Brief Theory)

### 1.1 Ορισμός Υπολογιστή και Προγράμματος
* **Υπολογιστής (Computer):** Μια καθολική μηχανή (universal machine) η οποία αποθηκεύει και επεξεργάζεται πληροφορίες υπό τον έλεγχο ενός μεταβαλλόμενου προγράμματος. 
* **Πρόγραμμα (Computer Program):** Ένα λεπτομερές, βήμα προς βήμα σύνολο οδηγιών που υπαγορεύει στον υπολογιστή τις ακριβείς ενέργειες που πρέπει να εκτελέσει.
* **Υλικό (Hardware):** Τα φυσικά, χειροπιαστά εξαρτήματα της υπολογιστικής μηχανής.
* **Λογισμικό (Software):** Τα προγράμματα που εκτελούνται από το υλικό και καθορίζουν τη λειτουργικότητα του συστήματος. Χωρίς λογισμικό, το υλικό είναι αδρανές.
* **Προγραμματισμός (Programming):** Η διαδικασία σχεδίασης, συγγραφής και υλοποίησης λογισμικού.

### 1.2 Αρχιτεκτονική και Λειτουργία Υλικού
Ένα τυπικό ψηφιακό υπολογιστικό σύστημα αποτελείται από πέντε βασικές λειτουργικές μονάδες:
1. **Κεντρική Μονάδα Επεξεργασίας (CPU - Central Processing Unit):** Ο «εγκέφαλος» του υπολογιστή. Εκτελεί βασικές αριθμητικές (π.χ. πρόσθεση) και λογικές πράξεις (π.χ. σύγκριση ισότητας).
2. **Κύρια Μνήμη (Main Memory / RAM):** Ταχεία, προσωρινή (volatile) μνήμη που αποθηκεύει τα προγράμματα και τα δεδομένα κατά τη διάρκεια της επεξεργασίας τους. Τα περιεχόμενά της χάνονται όταν διακοπεί η τροφοδοσία ρεύματος.
3. **Δευτερεύουσα Μνήμη (Secondary Memory):** Μόνιμη (non-volatile) μνήμη αποθήκευσης (π.χ. σκληροί δίσκοι HDD, δίσκοι SSD, μνήμες Flash).
4. **Συσκευές Εισόδου (Input Devices):** Μέσα μέσω των οποίων ο χρήστης εισάγει πληροφορίες (π.χ. πληκτρολόγιο, ποντίκι).
5. **Συσκευές Εξόδου (Output Devices):** Μέσα παρουσίασης των αποτελεσμάτων της επεξεργασίας στον χρήστη (π.χ. οθόνη, εκτυπωτής).

```text
       +------------------+         +-------------------+
       | Συσκευές Εισόδου | ------> |        CPU        |
       +------------------+         |  (Κεντρική Μονάδα |
                                    |    Επεξεργασίας)  |
       +------------------+         +---------^---------+
       | Συσκευές Εξόδου  | <-------|---------|---------+
       +------------------+         |         |         |
                                  +-v---------v-+     +-v----------+
                                  |    Κύρια    |     |Δευτερεύουσα|
                                  |    Μνήμη    | <-> |   Μνήμη    |
                                  |    (RAM)    |     | (HDD/SSD)  |
                                  +-------------+     +------------+
```
* **Κύκλος Ανάκλησης-Εκτέλεσης (Fetch-Execute Cycle):** Η CPU λειτουργεί αδιάκοπα εκτελώντας τον εξής τριφασικό κύκλο:
  $$\text{Ανάκληση (Fetch)} \longrightarrow \text{Αποκωδικοποίηση (Decode)} \longrightarrow \text{Εκτέλεση (Execute)}$$

### 1.3 Επιστήμη των Υπολογιστών και Αλγόριθμοι
* **Επιστήμη των Υπολογιστών (Computer Science):** Η μελέτη των διαδικασιών που μπορούν να περιγραφούν και να υπολογιστούν. Το κεντρικό ερώτημα της επιστήμης είναι: *«Τι μπορεί να υπολογιστεί;»* (What can be computed?).
* **Αλγόριθμος (Algorithm):** Μια σαφώς ορισμένη, πεπερασμένη σειρά βημάτων (μια «συνταγή») για την επίλυση ενός συγκεκριμένου προβλήματος.
* **Μη Διαχειρίσιμα Προβλήματα (Intractable Problems):** Προβλήματα για τα οποία υπάρχουν αλγόριθμοι επίλυσης, αλλά απαιτούν υπερβολικά μεγάλο χρόνο ή μνήμη για να εκτελεστούν, καθιστώντας τα πρακτικά αδύνατα προς επίλυση για μεγάλες εισόδους.

### 1.4 Γλώσσες Προγραμματισμού και Μετάφραση
* **Γλώσσα Μηχανής (Machine Language):** Η χαμηλότερου επιπέδου γλώσσα, αποτελούμενη αποκλειστικά από δυαδικά ψηφία ($0$ και $1$), την οποία κατανοεί άμεσα το υλικό της CPU.
* **Γλώσσα Υψηλού Επιπέδου (High-Level Language):** Ανθρωποκεντρική, φορητή γλώσσα (π.χ. Python, C++, Java) που επιτρέπει τη σύνταξη αλγορίθμων με τρόπο κατανοητό από τον άνθρωπο.
* **Μεταγλωττιστής (Compiler):** Πρόγραμμα που μεταφράζει ολόκληρο τον πηγαίο κώδικα (source code) μιας γλώσσας υψηλού επιπέδου σε γλώσσα μηχανής μία φορά, παράγοντας ένα αυτόνομο εκτελέσιμο αρχείο.
* **Διερμηνέας (Interpreter):** Πρόγραμμα που προσομοιώνει έναν υπολογιστή, αναλύοντας και εκτελώντας τον πηγαίο κώδικα απευθείας, γραμμή προς γραμμή.
* **Μοντέλο Python:** Η Python χρησιμοποιεί ένα υβριδικό σύστημα. Ο πηγαίος κώδικας (`.py`) μεταγλωττίζεται πρώτα σε μια ενδιάμεση μορφή που ονομάζεται **κώδικας byte (byte code)** (`.pyc`) και στη συνέχεια εκτελείται από τον διερμηνέα της εικονικής μηχανής της Python (Python Virtual Machine).

### 1.5 Χαοτικά Μοντέλα και Υπολογισμοί
* **Logistic Map (Εξίσωση Logistic):** Μια απλή, μη γραμμική μαθηματική εξίσωση που μοντελοποιεί δυναμικά συστήματα (π.χ. πληθυσμούς ή ηλεκτρονικά κυκλώματα):
  $$x_{n+1} = k \cdot x_n \cdot (1 - x_n)$$
  όπου $x_n \in [0, 1]$ η κατάσταση στο βήμα $n$, και $k$ μια σταθερά (παράμετρος ελέγχου).
* **Χάος (Chaos):** Φαινόμενο κατά το οποίο ένα απολύτως ντετερμινιστικό σύστημα παρουσιάζει απρόβλεπτη συμπεριφορά και **ακραία ευαισθησία στις αρχικές συνθήκες** (φαινόμενο της πεταλούδας / butterfly effect). Μια απειροελάχιστη διαφορά στην αρχική τιμή $x_0$ οδηγεί γρήγορα σε τελείως διαφορετικά αποτελέσματα.

---

## 2. Λυμένα Προβλήματα (Solved Problems)

### Πρόβλημα 1
**Εκφώνηση:** Εξηγήστε γιατί η κύρια μνήμη (RAM) χαρακτηρίζεται ως «πτητική» (volatile) και πώς αυτή η ιδιότητα διαφοροποιείται από τα χαρακτηριστικά της δευτερεύουσας μνήμης κατά την εκτέλεση ενός προγράμματος.
**Λύση:**
1. Η RAM χαρακτηρίζεται ως **πτητική (volatile)** επειδή απαιτεί συνεχή παροχή ηλεκτρικού ρεύματος για να διατηρήσει τα δεδομένα της. Όταν ο υπολογιστής απενεργοποιείται, όλα τα περιεχόμενα της RAM διαγράφονται οριστικά.
2. Αντίθετα, η δευτερεύουσα μνήμη (π.χ. ένας δίσκος SSD ή ένας σκληρός δίσκος) είναι **μη πτητική (non-volatile)**, πράγμα που σημαίνει ότι αποθηκεύει τα δεδομένα μόνιμα, ακόμη και χωρίς ηλεκτρικό ρεύμα.
3. Κατά την εκτέλεση ενός προγράμματος, συμβαίνουν τα εξής βήματα:
   * Το πρόγραμμα είναι αποθηκευμένο μόνιμα στη δευτερεύουσα μνήμη.
   * Με την εκκίνησή του, οι οδηγίες και τα δεδομένα του αντιγράφονται (φορτώνονται) στη γρήγορη κύρια μνήμη (RAM).
   * Η CPU επικοινωνεί απευθείας με τη RAM για να ανακτήσει και να εκτελέσει αυτές τις οδηγίες, καθώς η ταχύτητα της δευτερεύουσας μνήμης είναι πολύ χαμηλή για τις ανάγκες επεξεργασίας της CPU.

---

### Πρόβλημα 2
**Εκφώνηση:** Συγκρίνετε τον μεταγλωττιστή (compiler) με τον διερμηνέα (interpreter) ως προς τη διαδικασία μετάφρασης, την ταχύτητα εκτέλεσης και τη φορητότητα του κώδικα.
**Λύση:**
Μπορούμε να αναλύσουμε τις διαφορές τους με βάση τρεις άξονες:
1. **Διαδικασία Μετάφρασης:**
   * Ο **compiler** λαμβάνει ολόκληρο τον πηγαίο κώδικα (source code) και τον μεταφράζει σε ένα αυτόνομο αρχείο γλώσσας μηχανής (binary executable) πριν από την εκτέλεση.
   * Ο **interpreter** δεν παράγει εκτελέσιμο αρχείο. Διαβάζει, μεταφράζει και εκτελεί τον κώδικα γραμμή προς γραμμή κατά τον χρόνο εκτέλεσης (runtime).
2. **Ταχύτητα Εκτέλεσης:**
   * Τα μεταγλωττισμένα προγράμματα εκτελούνται πολύ ταχύτερα, διότι η μετάφραση έχει ολοκληρωθεί εκ των προτέρων και η CPU εκτελεί απευθείας τις οδηγίες μηχανής.
   * Τα διερμηνευόμενα προγράμματα είναι πιο αργά, καθώς η διαδικασία της μετάφρασης γίνεται ταυτόχρονα με την εκτέλεση.
3. **Φορητότητα (Portability):**
   * Ο μεταγλωττισμένος κώδικας είναι άμεσα εξαρτημένος από την αρχιτεκτονική της CPU για την οποία παρήχθη (π.χ. ένα αρχείο `.exe` για Intel x86 δεν τρέχει σε ARM επεξεργαστή κινητού τηλεφώνου).
   * Ο διερμηνευόμενος κώδικας παρουσιάζει υψηλή φορητότητα. Το ίδιο αρχείο πηγαίου κώδικα (π.χ. ένα αρχείο `.py`) μπορεί να εκτελεστεί σε οποιαδήποτε πλατφόρμα, αρκεί να υπάρχει εγκατεστημένος ο κατάλληλος interpreter για τη συγκεκριμένη αρχιτεκτονική.

---

### Πρόβλημα 3
**Εκφώνηση:** Ποια είναι η διαφορά μεταξύ του συντακτικού (syntax) και της σημασιολογίας (semantics) μιας γλώσσας προγραμματισμού; Δώστε από ένα παράδειγμα συντακτικού και σημασιολογικού λάθους στην Python.
**Λύση:**
1. **Συντακτικό (Syntax):** Οι αυστηροί κανόνες γραμματικής και ορθογραφίας που καθορίζουν πώς πρέπει να σχηματίζονται οι εντολές στη γλώσσα. Αν υπάρχει συντακτικό λάθος, το πρόγραμμα αποτυγχάνει να ξεκινήσει την εκτέλεσή του.
   * *Παράδειγμα συντακτικού λάθους στην Python:*
     ```python
     print("Hello"
     ```
     (Λείπει η δεξιά παρένθεση κλεισίματος).
2. **Σημασιολογία (Semantics):** Το πραγματικό νόημα ή το αποτέλεσμα της εκτέλεσης των εντολών. Ένα πρόγραμμα μπορεί να είναι 100% συντακτικά ορθό, αλλά να μην κάνει αυτό που επιθυμεί ο προγραμματιστής λόγω λογικού σφάλματος.
   * *Παράδειγμα σημασιολογικού λάθους στην Python:*
     Αν θέλουμε να υπολογίσουμε τον μέσο όρο δύο αριθμών $a$ και $b$ και γράψουμε:
     ```python
     average = a + b / 2
     ```
     Συντακτικά ο κώδικας είναι σωστός. Σημασιολογικά όμως είναι λάθος, διότι λόγω προτεραιότητας πράξεων θα υπολογιστεί το $a + \frac{b}{2}$ αντί του $\frac{a + b}{2}$.

---

### Πρόβλημα 4
**Εκφώνηση:** Δίνεται το ακόλουθο τμήμα κώδικα σε Python:
```python
x = 10
y = 3
print("x + y =", x + y)
print("x", "+", "y", "=", 13)
```
Ποια θα είναι η ακριβής έξοδος στην οθόνη κατά την εκτέλεση; Αναλύστε τον ρόλο των ορισμάτων στη συνάρτηση `print()`.
**Λύση:**
1. **Ανάλυση 1ης εντολής `print("x + y =", x + y)`:**
   * Το πρώτο όρισμα `"x + y ="` είναι μια συμβολοσειρά (string literal) και τυπώνεται αυτούσιο.
   * Το δεύτερο όρισμα είναι η μαθηματική έκφραση `x + y`. Ο διερμηνέας ανακτά τις τιμές των μεταβλητών $x = 10$ και $y = 3$, υπολογίζει το άθροισμα $10 + 3 = 13$, και τυπώνει την τιμή $13$.
   * Η συνάρτηση `print()` διαχωρίζει αυτόματα τα ορίσματά της με ένα κενό διάστημα.
   * Έξοδος: `x + y = 13`
2. **Ανάλυση 2ης εντολής `print("x", "+", "y", "=", 13)`:**
   * Η συνάρτηση δέχεται πέντε ξεχωριστά ορίσματα: τέσσερα strings (`"x"`, `"+"`, `"y"`, `"="`) και έναν ακέραιο (`13`).
   * Θα τα τυπώσει στη σειρά, χωρίζοντάς τα με κενά.
   * Έξοδος: `x + y = 13`

Συνεπώς, η τελική έξοδος στην οθόνη είναι:
```text
x + y = 13
x + y = 13
```

---

### Πρόβλημα 5
**Εκφώνηση:** Αναλύστε την εξέλιξη των τιμών των μεταβλητών και προσδιορίστε την ακριβή έξοδο του παρακάτω βρόχου επανάληψης (loop):
```python
total = 1
for i in range(4):
    total = total * 2
    print(i, total)
```
**Λύση:**
Η συνάρτηση `range(4)` παράγει τη σειρά ακεραίων $[0, 1, 2, 3]$. Ο βρόχος θα εκτελεστεί ακριβώς 4 φορές. Ας παρακολουθήσουμε τις τιμές βήμα-βήμα:
* **Αρχικοποίηση:** $\text{total} = 1$.
* **1η Επανάληψη ($i = 0$):**
  * $\text{total} = 1 \cdot 2 = 2$.
  * Εκτυπώνεται το $i$ και το $\text{total}$. Έξοδος: `0 2`
* **2η Επανάληψη ($i = 1$):**
  * $\text{total} = 2 \cdot 2 = 4$.
  * Έξοδος: `1 4`
* **3η Επανάληψη ($i = 2$):**
  * $\text{total} = 4 \cdot 2 = 8$.
  * Έξοδος: `2 8`
* **4η Επανάληψη ($i = 3$):**
  * $\text{total} = 8 \cdot 2 = 16$.
  * Έξοδος: `3 16`

Η συνολική έξοδος του προγράμματος είναι:
```text
0 2
1 4
2 8
3 16
```

---

### Πρόβλημα 6
**Εκφώνηση:** Δίνεται ο ακόλουθος ορισμός συνάρτησης στην Python:
```python
def welcome(name):
    print("Welcome,", name)
```
Περιγράψτε τι θα συμβεί και ποια θα είναι η έξοδος στην οθόνη αν καλέσουμε:
1. `welcome("Alex")`
2. `welcome` (χωρίς παρενθέσεις)
**Λύση:**
3. **Κλήση `welcome("Alex")`:**
   * Η συνάρτηση καλείται κανονικά. Η συμβολοσειρά `"Alex"` μεταβιβάζεται ως όρισμα (argument) και αντιστοιχίζεται στην παράμετρο `name`.
   * Εκτελείται η εντολή `print("Welcome,", "Alex")`.
   * Έξοδος: `Welcome, Alex`
2. **Κλήση `welcome`:**
   * Χωρίς τις παρενθέσεις `()`, η συνάρτηση **δεν εκτελείται**. 
   * Το όνομα `welcome` αποτελεί απλώς μια αναφορά (pointer) στο αντικείμενο της συνάρτησης που είναι αποθηκευμένο στη μνήμη.
   * Ο διερμηνέας θα επιστρέψει τις πληροφορίες του αντικειμένου και τη διεύθυνση μνήμης του.
   * Έξοδος (ενδεικτική): `<function welcome at 0x7f9b1c28f1e0>`

---

### Πρόβλημα 7
**Εκφώνηση:** Ένας χρήστης εκτελεί ένα πρόγραμμα Python και του ζητείται εισαγωγή δεδομένων μέσω της εντολής `x = eval(input("Enter a value: "))`. 
1. Ποια είναι η διαφορά μεταξύ των συναρτήσεων `input()` και `eval()`;
2. Αν ο χρήστης πληκτρολογήσει `3 + 5`, ποια θα είναι η τιμή και ο τύπος της μεταβλητής `x`;
**Λύση:**
3. **Διαφορά `input()` και `eval()`:**
   * Η `input()` διαβάζει τα πλήκτρα που πατά ο χρήστης και τα επιστρέφει **πάντα** ως συμβολοσειρά (string), δηλαδή στην περίπτωση αυτή το `"3 + 5"`.
   * Η `eval()` (evaluate) λαμβάνει μια συμβολοσειρά και την αποτιμά (την εκτελεί) σαν να ήταν κώδικας Python.
2. **Αποτίμηση του `3 + 5`:**
   * Η `input()` επιστρέφει το string `"3 + 5"`.
   * Η `eval("3 + 5")` εκτελεί την πρόσθεση και παράγει το αποτέλεσμα $8$.
   * Η μεταβλητή `x` θα λάβει την τιμή $8$ και ο τύπος της θα είναι ακέραιος (`int`).

---

### Πρόβλημα 8
**Εκφώνηση:** Χρησιμοποιώντας τη χαοτική logistic map με παράμετρο $k = 4.0$ και αρχική τιμή $x_0 = 0.4$, υπολογίστε αναλυτικά τις επόμενες τρεις καταστάσεις $x_1, x_2, x_3$.
**Λύση:**
Η εξίσωση του συστήματος είναι:
$$x_{n+1} = 4.0 \cdot x_n \cdot (1 - x_n)$$

1. **Υπολογισμός $x_1$ (για $n=0$):**
   $$x_1 = 4.0 \cdot x_0 \cdot (1 - x_0)$$
   $$x_1 = 4.0 \cdot 0.4 \cdot (1 - 0.4) = 4.0 \cdot 0.4 \cdot 0.6$$
   $$x_1 = 1.6 \cdot 0.6 = 0.96$$

2. **Υπολογισμός $x_2$ (για $n=1$):**
   $$x_2 = 4.0 \cdot x_1 \cdot (1 - x_1)$$
   $$x_2 = 4.0 \cdot 0.96 \cdot (1 - 0.96) = 4.0 \cdot 0.96 \cdot 0.04$$
   $$x_2 = 3.84 \cdot 0.04 = 0.1536$$

3. **Υπολογισμός $x_3$ (για $n=2$):**
   $$x_3 = 4.0 \cdot x_2 \cdot (1 - x_2)$$
   $$x_3 = 4.0 \cdot 0.1536 \cdot (1 - 0.1536) = 4.0 \cdot 0.1536 \cdot 0.8464$$
   $$x_3 = 0.6144 \cdot 0.8464 \approx 0.520028$$

---

### Πρόβλημα 9
**Εκφώνηση:** Δείξτε πώς η logistic map εμφανίζει ευαισθησία στις αρχικές συνθήκες (butterfly effect). Συγκρίνετε τα αποτελέσματα για $k = 3.9$ και δύο πολύ κοντινές αρχικές τιμές, $x_0 = 0.50$ και $y_0 = 0.51$, υπολογίζοντας τα $x_1, x_2$ και $y_1, y_2$ με ακρίβεια τεσσάρων δεκαδικών ψηφίων.
**Λύση:**
Η εξίσωση είναι: $z_{n+1} = 3.9 \cdot z_n \cdot (1 - z_n)$.

1. **Πρώτο Σύστημα ($x_0 = 0.50$):**
   * **Βήμα 1:**
     $$x_1 = 3.9 \cdot 0.50 \cdot (1 - 0.50) = 3.9 \cdot 0.50 \cdot 0.50 = 0.9750$$
   * **Βήμα 2:**
     $$x_2 = 3.9 \cdot 0.9750 \cdot (1 - 0.9750) = 3.9 \cdot 0.9750 \cdot 0.0250 = 0.0951$$

2. **Δεύτερο Σύστημα ($y_0 = 0.51$):**
   * **Βήμα 1:**
     $$y_1 = 3.9 \cdot 0.51 \cdot (1 - 0.51) = 3.9 \cdot 0.51 \cdot 0.49 = 0.9746$$
   * **Βήμα 2:**
     $$y_2 = 3.9 \cdot 0.9746 \cdot (1 - 0.9746) = 3.9 \cdot 0.9746 \cdot 0.0254 \approx 0.0965$$

3. **Σύγκριση:**
   * Η αρχική διαφορά ήταν: $|x_0 - y_0| = |0.5000 - 0.5100| = 0.0100$.
   * Στο βήμα 1, η διαφορά είναι: $|x_1 - y_1| = |0.9750 - 0.9746| = 0.0004$.
   * Στο βήμα 2, η διαφορά είναι: $|x_2 - y_2| = |0.0951 - 0.0965| = 0.0014$.
   * Παρατήρηση: Αν και στο βήμα 1 η διαφορά μειώθηκε προσωρινά, στο βήμα 2 άρχισε πάλι να μεγαλώνει. Σε βάθος 10-15 επαναλήψεων, οι τιμές των δύο συστημάτων θα αποκλίνουν εντελώς και δεν θα παρουσιάζουν καμία συσχέτιση, αποδεικνύοντας τη χαοτική συμπεριφορά του μοντέλου.

---

### Πρόβλημα 10
**Εκφώνηση:** Αναλύστε τη δομή και τη ροή εκτέλεσης του παρακάτω προγράμματος Python. Τι κάνει η πρώτη γραμμή, πώς ορίζεται ο κύριος κορμός και πώς εκτελείται;
```python
# File: power.py

def main():
    print("This program calculates powers of 2")
    for i in range(3):
        val = 2 ** i
        print(val)

main()
```
**Λύση:**
1. **Γραμμή `# File: power.py`:** Είναι ένα **σχόλιο (comment)**. Ο διερμηνέας της Python αγνοεί οποιοδήποτε κείμενο ξεκινά με τον χαρακτήρα `#` μέχρι το τέλος της γραμμής. Χρησιμεύει μόνο για την ανάγνωση από ανθρώπους.
2. **Ορισμός Συνάρτησης (`def main():`):** Με τη λέξη-κλειδί `def`, ορίζεται μια νέα συνάρτηση με το όνομα `main`. Όλες οι εντολές που είναι εσοχή (indented) κάτω από το `def` αποτελούν το σώμα της συνάρτησης. Όταν φορτώνεται το αρχείο, ο κώδικας αυτός αποθηκεύεται στη μνήμη αλλά **δεν εκτελείται ακόμα**.
3. **Κορμός της `main`:**
   * Τυπώνει το εισαγωγικό μήνυμα.
   * Εκτελεί έναν βρόχο `for` με το $i$ να παίρνει τις τιμές $0, 1, 2$.
   * Υπολογίζει το $2^i$ (ο τελεστής `**` συμβολίζει την ύψωση σε δύναμη) και το εκχωρεί στη μεταβλητή `val`.
   * Τυπώνει την τιμή `val`.
4. **Κλήση Συνάρτησης (`main()`):** Είναι η τελευταία γραμμή του αρχείου. Δεν έχει εσοχή, οπότε εκτελείται αμέσως μόλις φορτωθεί το αρχείο. Καλεί τη συνάρτηση `main` να εκτελέσει τις οδηγίες της.
5. **Έξοδος Προγράμματος:**
   ```text
   This program calculates powers of 2
   1
   2
   4
   ```

---

## 3. Συμπληρωματικά / Άλυτα Προβλήματα (Supplementary Problems)

### Πρόβλημα 1
Ένας φοιτητής γράφει το παρακάτω πρόγραμμα:
```python
a = 15
b = 5
print("b - a =", b - a)
```
Ποιο θα είναι το αποτέλεσμα που θα τυπωθεί στην οθόνη;
(Απάντηση: `b - a = -10`)

### Πρόβλημα 2
Υπολογίστε την τιμή $x_1$ της logistic map $x_{n+1} = 3.8 \cdot x_n \cdot (1 - x_n)$ αν η αρχική τιμή εισόδου είναι $x_0 = 0.3$.
(Απάντηση: $0.798$)

### Πρόβλημα 3
Ποια θα είναι η τελική τιμή της μεταβλητής `result` μετά την εκτέλεση του παρακάτω βρόχου;
```python
result = 0
for i in range(5):
    result = result + i
```
(Απάντηση: $10$)

### Πρόβλημα 4
Αν εκτελέσουμε στην Python την εντολή:
```python
print("5" + "10")
```
ποια θα είναι η έξοδος στην οθόνη;
(Απάντηση: `510`)

### Πρόβλημα 5
Δίνεται ο ακόλουθος κώδικας:
```python
def multiply(x):
    return x * 3

print(multiply(multiply(2)))
```
Ποια τιμή θα τυπωθεί στην οθόνη;
(Απάντηση: $18$)
